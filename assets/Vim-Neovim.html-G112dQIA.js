import{_ as o,r as t,o as i,c as l,b as e,d as n,a as s,e as c}from"./app-jDLxG9B3.js";const p={},r=e("h1",{id:"vim-neovim",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#vim-neovim","aria-hidden":"true"},"#"),n(" Vim - Neovim")],-1),d={href:"https://alpha2phi.medium.com/learn-neovim-the-practical-way-8818fcf4830f",target:"_blank",rel:"noopener noreferrer"},u=e("code",null,"init.lua",-1),m={href:"https://www.vim.org/",target:"_blank",rel:"noopener noreferrer"},h={href:"https://github.com/neovim/neovim",target:"_blank",rel:"noopener noreferrer"},k=c(`<p>Once Vim is opened Ex commands are executed</p><h2 id="initialization" tabindex="-1"><a class="header-anchor" href="#initialization" aria-hidden="true">#</a> Initialization</h2><p>First, initialize (if defined) the system <code>vimrc</code> (see <code>:version</code>)</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token variable">$VIM</span>/vimrc
</code></pre></div><p>Then, initialize the <strong>first found</strong> of the following user <code>vimrc</code> configurations:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token variable">$VIMINIT</span>
<span class="token environment constant">$HOME</span>/.vimrc
<span class="token environment constant">$HOME</span>/.vim/vimrc
<span class="token variable">$EXINIT</span>
<span class="token environment constant">$HOME</span>/.exrc
<span class="token variable">$VIMRUNTIME</span>/defaults.vim
</code></pre></div><p>For Neovim:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>system vimrc file: <span class="token string">&quot;<span class="token variable">$VIM</span>/sysinit.vim&quot;</span>
fall-back <span class="token keyword">for</span> <span class="token variable">$VIM</span><span class="token builtin class-name">:</span> <span class="token string">&quot;/usr/share/nvim&quot;</span>
</code></pre></div><div class="custom-container tip"><p class="custom-container-title">NO USER CONFIGURATION</p><p>To launch Vim/Neovim without user defined configuration file:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># skip everything</span>
<span class="token punctuation">{</span> vim/nvim <span class="token punctuation">}</span> <span class="token parameter variable">-u</span> NONE
<span class="token comment"># skip everything except plugins and syntax highlighting</span>
<span class="token punctuation">{</span> vim/nvim <span class="token punctuation">}</span> <span class="token parameter variable">-u</span> NORC
</code></pre></div></div><div class="custom-container tip"><p class="custom-container-title">NOTE</p><ul><li>Commands which are executed after entering <code>:</code> are called <em>Ex</em> commands</li><li>Environment variables such as <code>$VIM</code> or <code>$VIMRUNTIME</code> are set on launch of vim. They are provided as fallback paths. (see <code>:version</code>, <code>:echo $VIM</code>)</li></ul></div><h3 id="plugins" tabindex="-1"><a class="header-anchor" href="#plugins" aria-hidden="true">#</a> Plugins</h3><p>Plugins / Package Scripts are loaded next:</p><div class="language-vim" data-ext="vim"><pre class="language-vim"><code><span class="token punctuation">:</span><span class="token keyword">h</span> load<span class="token operator">-</span>plugins
</code></pre></div><p>First, plugin scripts are loaded, which is equivalent to running:</p><div class="language-vim" data-ext="vim"><pre class="language-vim"><code><span class="token punctuation">:</span><span class="token keyword">runtime</span><span class="token operator">!</span> plugin<span class="token operator">/</span><span class="token operator">*</span><span class="token operator">*</span><span class="token operator">/</span><span class="token operator">*</span><span class="token operator">.</span><span class="token keyword">vim</span>
# except <span class="token keyword">for</span> <span class="token string">&#39;rtp&#39;</span> directories ending <span class="token string">&#39;after&#39;</span>
</code></pre></div><ul><li><strong>Usually, this is</strong> <code>$HOME/.vim/plugin/**/*.vim</code></li><li>Then, packages are loaded:</li><li><strong>Usually, this is</strong> <code>$HOME/.vim/pack/*/start/*/plugin/**/*.vim</code></li></ul><h3 id="runtimepath" tabindex="-1"><a class="header-anchor" href="#runtimepath" aria-hidden="true">#</a> Runtimepath</h3><p>A List of directories which will be searched for runtime files (see <code>:h rtp</code>)</p><p>To list current runtimepaths:</p><div class="language-vim" data-ext="vim"><pre class="language-vim"><code><span class="token punctuation">:</span><span class="token keyword">set</span> <span class="token builtin">rtp</span><span class="token operator">?</span>
</code></pre></div><p>The runtimepath search order:</p><ol><li>User <code>$HOME/.vim</code> or <code>$HOME/.config/nvim</code> (for Neovim)</li><li>Sysadmin folder</li><li><code>$VIMRUNTIME</code></li><li>Sysadmin <code>after</code> folder</li><li>User Home <code>after</code> folder</li></ol><p>Sample list of files and directories searched during runtime:</p><ul><li><code>syntax/</code></li><li><code>compiler/</code></li><li><code>ftdetect/</code></li><li><code>filetype.vim</code></li><li><code>scripts.vim</code></li><li><code>autoload/</code></li><li><code>ftplugin/</code></li><li><code>plugin/</code></li><li><code>pack/</code></li><li><code>colors/</code></li><li><code>doc/</code></li><li><code>import/</code></li><li><code>keymap/</code></li><li><code>lang/</code></li><li><code>menu.vim</code></li><li><code>print/</code></li><li><code>spell/</code></li><li><code>tutor</code></li><li><code>indent/</code></li></ul><h4 id="syntax" tabindex="-1"><a class="header-anchor" href="#syntax" aria-hidden="true">#</a> Syntax</h4><p>The runtime directory which is searched is called <code>syntax/</code></p><p>It is the first directory to be loaded from the runtimepath</p><p>Enables customized syntax highlighting</p><ul><li>When invoking <code>:set sytax=xxx</code>, syntax file(s) named <code>xxx.vim</code> are sourced from the runtimepath, Usually, an autocommand does this if the file type can be inferred</li><li>System defaults usually defined in <code>$VIMRUNTIME/syntax</code></li></ul><p>There are two ways to enable syntax:</p><ol><li><code>:syntax enable</code>: Enables syntax and mostly keeps the colours (highlight groups)</li><li><code>:syntax on</code>: Enables syntax but reverts to default colours (highlight groups)</li></ol><p>To create a custom syntax file:</p><ol><li><p>Create <code>syntax</code> folder inside <code>$HOME/.vim</code> or <code>$HOME/.config/nvim</code> (for Neovim)</p></li><li><p>Create a file such as <code>syntax/sshknownhosts.vim</code></p></li><li><p>Now, create a syntax group which match the string (keyword) or patterns (using regex)</p></li><li><p>Link the above syntax group with the highlight group (to get the highlight groups run <code>:hi</code>)</p><div class="language-vim" data-ext="vim"><pre class="language-vim"><code># <span class="token keyword">first</span> <span class="token builtin">syntax</span> group
<span class="token builtin">syntax</span> <span class="token keyword">match</span> sshknownhostspubkey <span class="token string">&quot;AAAA[0-9a-zA-Z+/]\\+[=]\\{0,2}&quot;</span>

# link the above <span class="token builtin">syntax</span> group <span class="token keyword">to</span> a <span class="token builtin">highlight</span> group
<span class="token builtin">highlight</span> <span class="token builtin">def</span> link sshknownhostspubkey Special

# keep adding <span class="token builtin">syntax</span> groups
<span class="token builtin">syn</span> keyword sshalg ssh<span class="token operator">-</span>rsa
<span class="token builtin">hi</span> <span class="token builtin">def</span> link sshalg Identifier

<span class="token builtin">syn</span> <span class="token keyword">match</span> sshknownhostsip <span class="token string">&quot;\\&lt;\\(\\d\\{1,3}\\.\\)\\{3}\\d\\{1,3}\\&gt;&quot;</span>
<span class="token builtin">hi</span> <span class="token builtin">def</span> link sshknownhostsip Constant
</code></pre></div></li><li><p>Now set the syntax type same as the above created filename: <code>:set syntax=sshknownhosts</code></p></li></ol><p>Some considerations:</p><ol><li><p>If <code>keyword</code> doesn&#39;t set the syntax properly, then probably vim doesn&#39;t know that match the keyword pattern</p><ul><li><p>Check the keyword pattern: <code>:set iskeyword?</code></p></li><li><p>Add the missing pattern to <code>iskeyword</code> at the start of the syntax file:</p><div class="language-vim" data-ext="vim"><pre class="language-vim"><code><span class="token comment">&quot; add - to match </span><span class="token string">&#39;ssh-rsa&#39;</span> keyword
<span class="token keyword">setlocal</span> <span class="token builtin">iskeyword</span><span class="token operator">=</span>@<span class="token punctuation">,</span><span class="token number">48</span><span class="token operator">-</span><span class="token number">57</span><span class="token punctuation">,</span>_<span class="token punctuation">,</span><span class="token number">192</span><span class="token operator">-</span><span class="token number">255</span><span class="token punctuation">,</span><span class="token operator">-</span>
</code></pre></div></li></ul></li><li><p>If you switch buffers the syntax highlighting will be gone. This is because, there is no automatic file type syntax inference going on (see <a href="#file-type-detection">File Type Detections</a></p></li></ol><h4 id="file-type-detection" tabindex="-1"><a class="header-anchor" href="#file-type-detection" aria-hidden="true">#</a> File Type Detection</h4><p>There are many ways to detect file types:</p><ol><li><p>Override file type <strong>AFTER</strong> default file type detection. Add <code>:set filetype=&lt;file&gt;</code> autocommand to a file in <code>ftdetect/</code></p></li><li><p>Only create a new file type <strong>AFTER</strong> default file type detection. Add <code>:setfiletype &lt;file&gt;</code> autocommand to a file in <code>ftdetect/</code></p></li><li><p>Define file type <strong>BEFORE</strong> default file type detection (detectable <em>by name</em>). Add <code>:setfiletype &lt;file&gt;</code> for all files you want to define in <code>filetype.vim</code> file</p></li><li><p>Define file type <strong>BEFORE</strong> default file type detection (detectable <em>by content</em>). Add <code>:setfiletype &lt;file&gt;</code> for all files you want to define in <code>scripts.vim</code> file</p></li></ol><p>Runtime files and directories searched for file type detection:</p><ul><li><code>ftdetect</code> +</li><li><code>filetype.vim</code> +</li><li><code>scripts.vim</code></li></ul><p>You can define your own file type using any one of the above mentioned ways (see <code>:h new-filetype</code>)</p><p><em>Example:</em></p><ul><li><p>Create <code>filetype.vim</code> file inside user vim runtime (using option 3)</p></li><li><p>Add an autocommand (commands that get executed when an event occurs)</p><div class="language-vim" data-ext="vim"><pre class="language-vim"><code>au BufNewFile<span class="token punctuation">,</span>BufRead know_hosts <span class="token keyword">setfiletype</span> sshknownshosts
</code></pre></div></li></ul><h4 id="auto-load" tabindex="-1"><a class="header-anchor" href="#auto-load" aria-hidden="true">#</a> Auto Load</h4><p>The directory <code>autoload/</code> is searched during runtime (see <code>:h autoload</code>)</p><ul><li>Scripts loaded only when required</li><li>When you issue a call <code>filename#funcname()</code>, Vim searches in the <code>autoload/</code> directory of runtimepath for a file called <code>filename.vim</code> and calls <code>funcname()</code></li></ul><p><em>Example:</em></p><ul><li><p>Create <code>autoload</code> directory inside the user vim runtime (using option 3)</p></li><li><p>Create a script file which performs some task(s), like <code>gcp.vim</code></p></li><li><p>Write your script in any of you preferred language like <code>Python</code>, <code>Vimscript</code>, <code>JavaScript</code>, <code>Lua</code>...</p><div class="language-vim" data-ext="vim"><pre class="language-vim"><code>echo <span class="token string">&quot;Initializing Autoload Plugin&quot;</span>
python3 <span class="token operator">&lt;</span><span class="token operator">&lt;</span> PYEND
from google<span class="token operator">.</span>cloud import storage

<span class="token builtin">def</span> <span class="token function">upload</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span>
    storage_client <span class="token operator">=</span> storage<span class="token operator">.</span><span class="token function">Client</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
    bucket <span class="token operator">=</span> storage_client<span class="token operator">.</span><span class="token function">bucket</span><span class="token punctuation">(</span><span class="token string">&#39;scripts&#39;</span><span class="token punctuation">)</span>
    blob <span class="token operator">=</span> bucket<span class="token operator">.</span><span class="token function">blob</span><span class="token punctuation">(</span><span class="token string">&#39;vim_demo&#39;</span><span class="token punctuation">)</span>
    blob<span class="token operator">.</span><span class="token function">upload_from_filename</span><span class="token punctuation">(</span><span class="token keyword">vim</span><span class="token operator">.</span><span class="token function">eval</span><span class="token punctuation">(</span><span class="token string">&quot;expand(&#39;%:p&#39;)&quot;</span><span class="token punctuation">)</span>
    <span class="token function">print</span><span class="token punctuation">(</span><span class="token string">&#39;UPLOADED&#39;</span><span class="token punctuation">)</span>

PYEND

<span class="token keyword">function</span> gcp#<span class="token function">Upload</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
    <span class="token punctuation">:</span>py3 <span class="token function">upload</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token keyword">endfunction</span>
</code></pre></div></li><li><p>Now call the above function using: <code>:call gcp#Upload()</code></p></li></ul><h4 id="compiler" tabindex="-1"><a class="header-anchor" href="#compiler" aria-hidden="true">#</a> Compiler</h4><p>Vim has builtin compiler options. Sets options for using a specific compiler (see <code>:h compiler</code>)</p><p>The directory <code>compiler/</code> is used to place compiler specific options.</p><ul><li>When invoking <code>:compiler xxx</code>, compiler specific options from <code>xxx.vim</code> are sourced to set a <code>makeprg</code> and <code>errorformat</code> for vim-native compilations</li></ul><p><em>Example:</em></p><ul><li><p>Create <code>compiler</code> directory inside the user vim runtime (using option 3)</p></li><li><p>Create a script file which contains the compiler instructions for a particular build, like <code>typescript.vim</code></p></li><li><p>Set the compiler, like <code>tsc</code> in case of TypeScript and set the error format (errors produced by the complier as <code>stdout</code>)</p><div class="language-vim" data-ext="vim"><pre class="language-vim"><code>CompilerSet <span class="token builtin">makeprg</span><span class="token operator">=</span>tsc
CompilerSet <span class="token builtin">errorformat</span><span class="token operator">=</span><span class="token operator">%</span>E<span class="token operator">%</span><span class="token function">f</span><span class="token punctuation">(</span><span class="token operator">%</span><span class="token keyword">l</span>\\\\<span class="token punctuation">,</span><span class="token operator">%</span><span class="token keyword">c</span><span class="token punctuation">)</span><span class="token punctuation">:</span><span class="token operator">%</span><span class="token keyword">m</span>
<span class="token comment">&quot; %E: Indicates error message</span>
<span class="token comment">&quot; %f: Indicates that this part is the filename (with full path)</span>
<span class="token comment">&quot; %l: Line number</span>
<span class="token comment">&quot; %c: Column number</span>
<span class="token comment">&quot; %m: Message part</span>
</code></pre></div></li><li><p>Now when we open a TypeScript project, just set the compiler, like <code>compiler typescript</code> (filename)</p></li><li><p>Then run make: It will build the project and add errors to the quick fix list (<code>:copen</code>)</p></li></ul><h4 id="file-type-plugin" tabindex="-1"><a class="header-anchor" href="#file-type-plugin" aria-hidden="true">#</a> File Type Plugin</h4><p>Defines Vim scripts loaded for particular files (applies to current buffer) (see <code>:h ftplugin</code>)</p><p>The directory <code>ftplugin/</code> is used to place the file specific vim scripts</p><p><em>Example:</em></p><ul><li><p>Create <code>ftplugin/</code> directory inside the user vim runtime (using option 3)</p></li><li><p>Create a script file, (<code>typescript.vim</code>) which sets file type specific options and keybindings</p></li><li><p>Now, just add the your options, keybindings, etc...</p><div class="language-vim" data-ext="vim"><pre class="language-vim"><code><span class="token comment">&quot; Set compiler automatically when the file is loaded</span>
<span class="token keyword">compiler</span> typescript
nnoremap <span class="token operator">&lt;</span><span class="token keyword">buffer</span><span class="token operator">&gt;</span> <span class="token operator">&lt;</span>space<span class="token operator">&gt;</span> <span class="token punctuation">:</span><span class="token keyword">silent</span> <span class="token keyword">make</span> <span class="token operator">&lt;</span>bar<span class="token operator">&gt;</span> <span class="token keyword">redraw</span><span class="token operator">!</span><span class="token operator">&lt;</span>CR<span class="token operator">&gt;</span>
<span class="token comment">&quot; &lt;buffer&gt;: Perfrom on the current buffer</span>
<span class="token comment">&quot; &lt;silent&gt;: Don&#39;t show stdout</span>
<span class="token comment">&quot; &lt;bar&gt;: Sperate 2 commands</span>
<span class="token comment">&quot; redraw!: Redraws the current screen (and removes any issues while displaying the buffer)</span>
</code></pre></div></li></ul><h4 id="plugin" tabindex="-1"><a class="header-anchor" href="#plugin" aria-hidden="true">#</a> Plugin</h4><p>Defines global plugins automatically loaded when vim starts (see <code>:h plugin</code>)</p><p>The directory <code>plugin/</code> is used to place the file specific vim scripts</p><p><em>Example:</em> Create plugin for OSC 52 yanks</p><ul><li>We want to create a plugin that allows us to directly yank all text to our local clipboard by utilizing an xterm-compatible terminal&#39;s OSC-52 escape sequence support</li></ul><div class="custom-container tip"><p class="custom-container-title">YANKING</p><p>For future use, add <code>alias vi=&#39;vi -c &quot;let g:tty=&#39;\\&#39;&#39;$(tty)&#39;\\&#39;&#39;&quot;&#39;</code> to <code>~/.bashrc</code> or <code>~/.zshrc</code></p></div><h2 id="registers" tabindex="-1"><a class="header-anchor" href="#registers" aria-hidden="true">#</a> Registers</h2><p>Types of registers:</p><ol><li><code>&quot;&quot;</code>: The <em>unnamed</em> register (last deleted, changed, or yanked content)</li><li><code>&quot;*</code>: synchronized with the selection clipboard</li><li><code>&quot;+</code>: synchronized with the system clipboard</li><li><code>&quot;_</code>: <em>black hole register</em> (deleted or changed content smaller than one line)</li><li><code>&quot;:</code>: most recent executed command</li><li><code>&quot;.</code>: last inserted text</li><li><code>&quot;%</code>: current filename</li><li><code>&quot;#</code>: alternate file name for the current window</li><li><code>&quot;/</code>: last search pattern</li><li><code>&quot;0 to &quot;9</code>: 10 <em>numbered registers</em> (last yank)</li><li><code>&quot;-</code>: <em>small delete register</em></li><li><code>a-z or A-Z</code>: 26 <em>named registers</em></li></ol><h2 id="reference" tabindex="-1"><a class="header-anchor" href="#reference" aria-hidden="true">#</a> Reference</h2>`,69),f={href:"https://shapeshed.com/vim-statuslines/",target:"_blank",rel:"noopener noreferrer"},g={href:"https://alvinalexander.com/linux/vi-vim-editor-color-scheme-syntax/#possible-highlight-groups",target:"_blank",rel:"noopener noreferrer"},y={href:"https://stackoverflow.com/questions/1218390/what-is-your-most-productive-shortcut-with-vim",target:"_blank",rel:"noopener noreferrer"};function v(b,x){const a=t("ExternalLinkIcon");return i(),l("div",null,[r,e("ul",null,[e("li",null,[e("a",d,[n("dot-files configure - "),u,s(a)])])]),e("p",null,[e("a",m,[n("Vim"),s(a)]),n(" (a contraction of Vi IMproved) is a free and open-source, screen-based text editor program")]),e("p",null,[n("And "),e("a",h,[n("Neovim"),s(a)]),n(": A Vim-fork focused on extensibility and usability")]),k,e("ul",null,[e("li",null,[e("a",f,[n("Build your own Vim statusline"),s(a)])]),e("li",null,[e("a",g,[n("How to control/configure vim colors"),s(a)])]),e("li",null,[e("a",y,[n("Vim productive commands"),s(a)])])])])}const _=o(p,[["render",v],["__file","Vim-Neovim.html.vue"]]);export{_ as default};
