import{_ as t,r as p,o as r,c as l,b as a,d as e,a as n,e as o}from"./app-jDLxG9B3.js";const c={},d=o(`<h1 id="postgresql" tabindex="-1"><a class="header-anchor" href="#postgresql" aria-hidden="true">#</a> PostgreSQL</h1><p>PostgreSQL or also known as Postgres, is a free and open-source relational database management system. PostgreSQL features transactions with Atomicity, Consistency, Isolation, Durability (ACID) properties automatically updatable views, materialized views, triggers, foreign keys, and stored procedures. It is designed to handle a range of workloads, from single machines to data warehouses or web services with many concurrent users</p><p>Features:</p><ul><li>OLTP</li><li>Performance</li><li>MUCC</li><li>Continuity</li></ul><h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h2><p>PostgreSQL 12 on Ubuntu 20.04 LTS:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">apt</span> update
<span class="token function">sudo</span> <span class="token function">apt</span> <span class="token function">install</span> <span class="token parameter variable">-y</span> postgresql postgresql-contrib postgresql-client
<span class="token function">sudo</span> systemctl status postgresql.service
</code></pre></div><h3 id="install-deploy-postgres-on-kubernetes-with-zalando-postgres-operator" tabindex="-1"><a class="header-anchor" href="#install-deploy-postgres-on-kubernetes-with-zalando-postgres-operator" aria-hidden="true">#</a> Install/deploy Postgres on Kubernetes with Zalando Postgres Operator</h3>`,8),i={href:"https://github.com/zalando/postgres-operator",target:"_blank",rel:"noopener noreferrer"},u=a("p",null,"You can find some tutorials regarding deployment of the operator and how to work with it, in the link list below:",-1),h={href:"https://thedatabaseme.de/2022/03/13/keep-the-elefants-in-line-deploy-zalando-operator-on-your-kubernetes-cluster/",target:"_blank",rel:"noopener noreferrer"},g={href:"https://thedatabaseme.de/2022/03/26/backup-to-s3-configure-zalando-postgres-operator-backup-with-wal-g/",target:"_blank",rel:"noopener noreferrer"},k={href:"https://thedatabaseme.de/2022/05/03/restore-and-clone-from-s3-configure-zalando-postgres-operator-restore-with-wal-g/",target:"_blank",rel:"noopener noreferrer"},m=o(`<h2 id="initial-database-connection" tabindex="-1"><a class="header-anchor" href="#initial-database-connection" aria-hidden="true">#</a> Initial database connection</h2><p>A local connection (from the database server) can be done by the following command:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token parameter variable">-u</span> postgres psql
psql <span class="token punctuation">(</span><span class="token number">12.12</span> <span class="token punctuation">(</span>Ubuntu <span class="token number">12.12</span>-0ubuntu0.20.04.1<span class="token punctuation">))</span>
Type <span class="token string">&quot;help&quot;</span> <span class="token keyword">for</span> help.
<span class="token assign-left variable">postgres</span><span class="token operator">=</span><span class="token comment">#</span>
</code></pre></div><h2 id="set-password-for-postgres-database-user" tabindex="-1"><a class="header-anchor" href="#set-password-for-postgres-database-user" aria-hidden="true">#</a> Set password for postgres database user</h2><p>The password for the <code>postgres</code> database user can be set the the quick command <code>\\password</code> or by <code>alter user postgres password &#39;Supersecret&#39;</code>. A connection using the <code>postgres</code> user is still not possible from the &quot;outside&quot; hence to the default settings in the <code>pg_hba.conf</code></p><h3 id="update-pg-hba-conf-to-allow-postgres-user-connections-with-password" tabindex="-1"><a class="header-anchor" href="#update-pg-hba-conf-to-allow-postgres-user-connections-with-password" aria-hidden="true">#</a> Update <code>pg_hba.conf</code> to allow postgres user connections with password</h3><p>In order to allow connections of the <code>postgres</code> database user not using OS user authentication, you have to update the <code>pg_hba.conf</code> which can be found under <code>/etc/postgresql/12/main/pg_hba.conf</code></p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">vi</span> /etc/postgresql/12/main/pg_hba.conf

<span class="token punctuation">..</span>.
<span class="token builtin class-name">local</span>   all             postgres                                peer
<span class="token punctuation">..</span>.
</code></pre></div><p>Change the last section of the above line to <code>md5</code>:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">local</span>   all             postgres                                md5
</code></pre></div><p>A restart is required in order to apply the new configuration:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> systemctl restart postgresql
</code></pre></div><p>Now a connection from outside the database host is possible e.g.</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>psql <span class="token parameter variable">-U</span> postgres <span class="token parameter variable">-d</span> postgres <span class="token parameter variable">-h</span> databasehostname
</code></pre></div><h2 id="creation-of-additional-database-users" tabindex="-1"><a class="header-anchor" href="#creation-of-additional-database-users" aria-hidden="true">#</a> Creation of additional database users</h2><p>A database user can be created by the following command:</p><div class="language-sql" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">CREATE</span> <span class="token keyword">USER</span> myuser <span class="token keyword">WITH</span> encrypted password <span class="token string">&#39;Supersecret&#39;</span><span class="token punctuation">;</span>
<span class="token keyword">CREATE</span> ROLE
postgres<span class="token operator">=</span><span class="token comment"># \\du</span>
                                   List <span class="token keyword">of</span> roles
 Role name <span class="token operator">|</span>                         Attributes                         <span class="token operator">|</span> Member <span class="token keyword">of</span>
<span class="token comment">-----------+------------------------------------------------------------+-----------</span>
 myuser    <span class="token operator">|</span>                                                            <span class="token operator">|</span> {}
 postgres  <span class="token operator">|</span> Superuser<span class="token punctuation">,</span> <span class="token keyword">Create</span> role<span class="token punctuation">,</span> <span class="token keyword">Create</span> DB<span class="token punctuation">,</span> <span class="token keyword">Replication</span><span class="token punctuation">,</span> Bypass RLS <span class="token operator">|</span> {}
</code></pre></div><h2 id="creation-of-additional-databases" tabindex="-1"><a class="header-anchor" href="#creation-of-additional-databases" aria-hidden="true">#</a> Creation of additional databases</h2><p>One can create new Postgres databases within an instance. Therefore you can use the <code>psql</code> command to login (see above).</p><div class="language-sql" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">CREATE</span> <span class="token keyword">DATABASE</span> dbname OWNER myuser<span class="token punctuation">;</span>
<span class="token keyword">CREATE</span> <span class="token keyword">DATABASE</span>
postgres<span class="token operator">=</span><span class="token comment"># \\l</span>
                                  List <span class="token keyword">of</span> <span class="token keyword">databases</span>
   Name    <span class="token operator">|</span>  Owner   <span class="token operator">|</span> Encoding <span class="token operator">|</span>   <span class="token keyword">Collate</span>   <span class="token operator">|</span>    Ctype    <span class="token operator">|</span>   Access <span class="token keyword">privileges</span>
<span class="token comment">-----------+----------+----------+-------------+-------------+-----------------------</span>
 dbname    <span class="token operator">|</span> myuser   <span class="token operator">|</span> UTF8     <span class="token operator">|</span> en_US<span class="token punctuation">.</span>UTF<span class="token operator">-</span><span class="token number">8</span> <span class="token operator">|</span> en_US<span class="token punctuation">.</span>UTF<span class="token operator">-</span><span class="token number">8</span> <span class="token operator">|</span>
 postgres  <span class="token operator">|</span> postgres <span class="token operator">|</span> UTF8     <span class="token operator">|</span> en_US<span class="token punctuation">.</span>UTF<span class="token operator">-</span><span class="token number">8</span> <span class="token operator">|</span> en_US<span class="token punctuation">.</span>UTF<span class="token operator">-</span><span class="token number">8</span> <span class="token operator">|</span>
 template0 <span class="token operator">|</span> postgres <span class="token operator">|</span> UTF8     <span class="token operator">|</span> en_US<span class="token punctuation">.</span>UTF<span class="token operator">-</span><span class="token number">8</span> <span class="token operator">|</span> en_US<span class="token punctuation">.</span>UTF<span class="token operator">-</span><span class="token number">8</span> <span class="token operator">|</span> <span class="token operator">=</span>c<span class="token operator">/</span>postgres          <span class="token operator">+</span>
           <span class="token operator">|</span>          <span class="token operator">|</span>          <span class="token operator">|</span>             <span class="token operator">|</span>             <span class="token operator">|</span> postgres<span class="token operator">=</span>CTc<span class="token operator">/</span>postgres
 template1 <span class="token operator">|</span> postgres <span class="token operator">|</span> UTF8     <span class="token operator">|</span> en_US<span class="token punctuation">.</span>UTF<span class="token operator">-</span><span class="token number">8</span> <span class="token operator">|</span> en_US<span class="token punctuation">.</span>UTF<span class="token operator">-</span><span class="token number">8</span> <span class="token operator">|</span> <span class="token operator">=</span>c<span class="token operator">/</span>postgres          <span class="token operator">+</span>
           <span class="token operator">|</span>          <span class="token operator">|</span>          <span class="token operator">|</span>             <span class="token operator">|</span>             <span class="token operator">|</span> postgres<span class="token operator">=</span>CTc<span class="token operator">/</span>postgres
</code></pre></div><p>You can leave the <code>OWNER</code> section of the command, when doing so, the current user will become owner of the newly created database.</p><p>To change the owner of an existing database later, you can use the following command:</p><div class="language-sql" data-ext="sql"><pre class="language-sql"><code>postgres<span class="token operator">=</span><span class="token comment"># alter database dbname owner to myuser;</span>
<span class="token keyword">ALTER</span> <span class="token keyword">DATABASE</span>
</code></pre></div><h2 id="backup-and-restore" tabindex="-1"><a class="header-anchor" href="#backup-and-restore" aria-hidden="true">#</a> Backup and Restore</h2><p>There are near to endless combinations in tools and parameters to backup postgres databases. Below you can find some examples using the Postgres built-in tools <code>pgdump</code>, <code>pg_basebackup</code> and <code>pg_restore</code>.</p><h3 id="pg-dump-pg-dumpall" tabindex="-1"><a class="header-anchor" href="#pg-dump-pg-dumpall" aria-hidden="true">#</a> pg_dump / pg_dumpall</h3><p>Using <code>pg_dump</code> or <code>pg_dumpall</code> enables you to extract / export a PostgreSQL database(s) into a (SQL) script file or a custom archive file.</p><h4 id="pg-dump" tabindex="-1"><a class="header-anchor" href="#pg-dump" aria-hidden="true">#</a> pg_dump</h4><p>The following command creates a custom archive file from a database specified with <code>-d</code>. Using the <code>--create</code> option will include the SQL commands in the dump script that will create the database before importing it later. The <code>-Z 9</code> option in this example compresses the SQL script created with the highest available compression rate (<code>0-9</code>).</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pg_dump <span class="token parameter variable">-h</span> vmdocker <span class="token parameter variable">-U</span> awx <span class="token parameter variable">-d</span> awx <span class="token parameter variable">--create</span> <span class="token parameter variable">-f</span> <span class="token parameter variable">-Z</span> <span class="token number">9</span> /tmp/awx_dump.sql.gz
</code></pre></div><p>The following command creates a custom archive file from a database specified with <code>-d</code>. To export data in custom format, you have to specify so with the <code>-F c</code> option. Custom file dumps have the benefit, that they are compressed by default.</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pg_dump <span class="token parameter variable">-h</span> <span class="token operator">&lt;</span>pg_host<span class="token operator">&gt;</span> <span class="token parameter variable">-U</span> <span class="token operator">&lt;</span>username<span class="token operator">&gt;</span> <span class="token parameter variable">-d</span> <span class="token operator">&lt;</span>database<span class="token operator">&gt;</span> <span class="token parameter variable">-F</span> c <span class="token parameter variable">-f</span> /pg_dump/dumpfile.dmp
</code></pre></div><p>Custom format files can only be restored by <code>pg_restore</code> (see below). A SQL dump can be restored by using <code>psql</code>.</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>psql <span class="token parameter variable">-d</span> newdb <span class="token parameter variable">-f</span> db.sql
</code></pre></div>`,34),b=a("code",null,"pg_dump",-1),f={href:"https://www.postgresql.org/docs/current/app-pgdump.html",target:"_blank",rel:"noopener noreferrer"},w=o(`<h4 id="pg-dumpall" tabindex="-1"><a class="header-anchor" href="#pg-dumpall" aria-hidden="true">#</a> pg-dumpall</h4><p>A full dump of all databases of a Postgres instance can be done by <code>pg_dumpall</code>. It will include also user creation information.</p><p>A difference to <code>pg_dump</code>, you cannot choose for different output formats. <code>pg_dumpall</code> will always create a SQL script as output. Therefore, you don&#39;t need <code>pg_restore</code> for restoring a &quot;full&quot; dump. Only <code>psql</code> is needed (see below).</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pg_dumpall <span class="token parameter variable">-h</span> <span class="token operator">&lt;</span>pg_host<span class="token operator">&gt;</span> <span class="token parameter variable">-U</span> postgres <span class="token operator">&gt;</span> database.out
</code></pre></div><p>If you use password authentication it will ask for a password each time. It is convenient to have a <code>~/.pgpass</code> file or <code>PGPASSWORD</code> environment variable set.</p><p>So importing a full dump is really easy by the following <code>psql</code> command:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>psql <span class="token parameter variable">-h</span> <span class="token operator">&lt;</span>pg_host<span class="token operator">&gt;</span> <span class="token parameter variable">-f</span> databaseb.out <span class="token parameter variable">-U</span> postgres
</code></pre></div>`,7),_=a("code",null,"pg_dumpall",-1),v={href:"https://www.postgresql.org/docs/current/app-pg-dumpall.html",target:"_blank",rel:"noopener noreferrer"},y=o(`<h3 id="pg-restore" tabindex="-1"><a class="header-anchor" href="#pg-restore" aria-hidden="true">#</a> pg-restore</h3><p><code>pg_restore</code> can be used to restore custom file dumps created by <code>pg_dump</code>.</p><p>The following command will create the datbase (which has been dumped before).</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>pg_restore <span class="token parameter variable">-h</span> <span class="token operator">&lt;</span>pg_host<span class="token operator">&gt;</span> <span class="token parameter variable">-U</span> <span class="token operator">&lt;</span>pg_user<span class="token operator">&gt;</span> <span class="token parameter variable">-d</span> postgres <span class="token parameter variable">--create</span> <span class="token parameter variable">-F</span> c /tmp/db.dmp <span class="token parameter variable">-v</span>
</code></pre></div>`,4),x=a("code",null,"pg_restore",-1),q={href:"https://www.postgresql.org/docs/current/app-pgrestore.html",target:"_blank",rel:"noopener noreferrer"};function T(S,U){const s=p("ExternalLinkIcon");return r(),l("div",null,[d,a("p",null,[e('Postgres is probably the database which is most common on Cloud platforms and also, running on Kubernetes environments. There are several so called "Kubernetes Operators" which handle the deployment of Postgres clusters for you. One of it is the '),a("a",i,[e("Postgres Operator by Zalando"),n(s)])]),u,a("ul",null,[a("li",null,[a("p",null,[a("a",h,[e("Deploy Zalando Postgres Operator on your Kubernetes cluster"),n(s)])])]),a("li",null,[a("p",null,[a("a",g,[e("Configure Zalando Postgres Operator Backup with WAL-G"),n(s)])])]),a("li",null,[a("p",null,[a("a",k,[e("Configure Zalando Postgres Operator Restore with WAL-G"),n(s)])])])]),m,a("p",null,[e("A complete guide of "),b,e(" from the official documentation can be found "),a("a",f,[e("here"),n(s)]),e(".")]),w,a("p",null,[e("A complete guide of "),_,e(" from the official documentation can be found "),a("a",v,[e("here"),n(s)]),e(".")]),y,a("p",null,[e("A complete guide of "),x,e(" from the official documentation can be found "),a("a",q,[e("here"),n(s)])])])}const C=t(c,[["render",T],["__file","PostgreSQL.html.vue"]]);export{C as default};
