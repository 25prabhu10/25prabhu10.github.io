import{_ as r,r as i,o as l,c as d,b as t,d as e,a as s,w as a,e as o}from"./app-jDLxG9B3.js";const p={},c=o(`<h1 id="sorting-algorithms" tabindex="-1"><a class="header-anchor" href="#sorting-algorithms" aria-hidden="true">#</a> Sorting Algorithms</h1><ol><li><a href="#bubble-sort">Bubble</a>: <code>O(n^2)</code> (also known as Simple Sort)</li><li><a href="#insertion-sort">Insertion</a>: <code>O(n^2)</code></li><li><a href="#selection-sort">Selection</a>: <code>O(n^2)</code></li><li>[Heap Sort]: <code>O(n log n)</code></li><li><a href="#merge-sort">Merge Sort</a>: <code>O(n log n)</code></li><li><a href="#quick-sort">Quick Sort</a>: <code>O(n log n)</code></li><li>[Tree Sort]: <code>O(n log n)</code></li><li><a href="#shell-sort">Shell Sort</a>: <code>O(n^(3/2))</code></li><li><a href="#count-sort">Count Sort</a>: <code>O(n)</code></li><li>[Bucket/Bin Sort] : <code>O(n)</code></li><li>[Radix Sort] : <code>O(n)</code></li></ol><p>In the above list 1-8 are called <strong>Comparison based Sorts</strong>, And 9-11 are called <strong>Index based Sorts</strong></p><ul><li>Go to <a href="#overview">Overview</a></li></ul><p>Criteria For Analysis:</p><ol><li><p>Number of Comparisons</p></li><li><p>Number of Swaps</p></li><li><p>Adaptive: Less time to sort an already sorted list. An adaptive sorting algorithm is one that takes advantage of the existing order of the input data to improve its efficiency. If an algorithm is adaptive, it performs fewer comparisons or swaps when dealing with partially sorted data</p></li><li><p>Stability: A sorting algorithm is stable if it maintains the relative order of equal elements in the sorted output as they were in the original input. In other words, if you have two equal elements A and B, and A appears before B in the input, a stable sorting algorithm will ensure that A still appears before B in the sorted output</p></li><li><p>Extra Memory</p></li></ol><h2 id="bubble-sort" tabindex="-1"><a class="header-anchor" href="#bubble-sort" aria-hidden="true">#</a> Bubble Sort</h2><p>It is also known as <strong>Simple Sort</strong></p><p>Bubble Sort is a straightforward sorting algorithm that repeatedly steps through the list, compares adjacent elements, and swaps them if they are in the wrong order. The pass through the list is repeated until the list is sorted</p><p><strong>Steps</strong>:</p><ol><li><p>Start with an unsorted list of elements</p></li><li><p>Compare the first element with the second element. If the first element is greater than the second element, swap them</p></li><li><p>Move to the next pair of elements (second and third) and repeat step 2</p></li><li><p>Continue this process for all adjacent pairs of elements in the list, comparing and swapping as necessary. After the first pass, the largest element will have &quot;bubbled up&quot; to the end of the list</p></li><li><p>Repeat steps 2-4 for a total of n-1 passes, where n is the number of elements in the list. On each pass, the largest unsorted element will move to the end of the list</p></li><li><p>After completing all passes, the list will be sorted</p></li></ol><p><em>Pseudocode</em>:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>procedure bubbleSort(list)
    n = length(list)
    for i from 0 to n - 1
        for j from 0 to n - i - 2
            if list[j] &gt; list[j + 1]
                swap(list[j], list[j + 1])
            end if
        end for
    end for
end procedure
</code></pre></div><p><em>Example:</em></p><p>Let&#39;s say you have an unsorted list: <code>[5, 2, 9, 1, 5]</code></p><ol><li>Pass 1: <code>[2, 5, 1, 5, 9]</code></li><li>Pass 2: <code>[2, 1, 5, 5, 9]</code></li><li>Pass 3: <code>[1, 2, 5, 5, 9]</code></li></ol><ul><li>After three passes, the list is sorted</li></ul><p>For an array of size <code>n</code> at worst case:</p><ul><li>Number of Passes: <code>n-1</code></li><li>Max Number of Comparisons: <code>(n * (n - 1)) / 2</code> --&gt; <code>O(n^2)</code></li><li>Max Number of Swaps: <code>(n * (n - 1)) / 2</code> --&gt; <code>O(n^2)</code></li></ul><p>Time complexity:</p><ul><li><p>Best case: <strong><code>O(n)</code></strong></p></li><li><p>Worst case: <strong><code>O(n^2)</code></strong></p></li><li><p>By using a flag we can make it Adaptive</p></li><li><p>For an Adaptive sort it will perform:</p><ul><li>Comparisons: <code>n-1</code></li><li>Swaps: <code>0</code></li><li>Performance: <code>O(n)</code></li></ul></li><li><p>Bubble sort is <strong>Adaptive and Stable</strong></p></li></ul><p>It can be used to get some number of elements sorted, like <strong>finding the largest (or smallest if desc) number</strong> in 1 pass (the last element)</p><p><em>Implementations:</em></p><div class="language-c" data-ext="c"><pre class="language-c"><code><span class="token keyword">void</span> <span class="token function">Bubble_Sort</span><span class="token punctuation">(</span><span class="token keyword">int</span> A<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span> <span class="token keyword">int</span> n<span class="token punctuation">)</span>
<span class="token punctuation">{</span>
    <span class="token keyword">int</span> i<span class="token punctuation">,</span> j<span class="token punctuation">,</span> temp <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>
    <span class="token keyword">int</span> flag<span class="token punctuation">;</span>                           <span class="token comment">// For making the sort Adaptive</span>

    <span class="token keyword">for</span> <span class="token punctuation">(</span>i <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> i <span class="token operator">&lt;</span> n <span class="token operator">-</span> <span class="token number">1</span><span class="token punctuation">;</span> i<span class="token operator">++</span><span class="token punctuation">)</span>
    <span class="token punctuation">{</span>
        flag <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>

        <span class="token keyword">for</span> <span class="token punctuation">(</span>j <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> j <span class="token operator">&lt;</span> n <span class="token operator">-</span> i <span class="token operator">-</span> <span class="token number">1</span><span class="token punctuation">;</span> j<span class="token operator">++</span><span class="token punctuation">)</span>
        <span class="token punctuation">{</span>
            <span class="token keyword">if</span> <span class="token punctuation">(</span>A<span class="token punctuation">[</span>j<span class="token punctuation">]</span> <span class="token operator">&gt;</span> A<span class="token punctuation">[</span>j <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">)</span>
            <span class="token punctuation">{</span>
                temp <span class="token operator">=</span> A<span class="token punctuation">[</span>j <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
                A<span class="token punctuation">[</span>j <span class="token operator">+</span> <span class="token number">1</span><span class="token punctuation">]</span> <span class="token operator">=</span> A<span class="token punctuation">[</span>j<span class="token punctuation">]</span><span class="token punctuation">;</span>
                A<span class="token punctuation">[</span>j<span class="token punctuation">]</span> <span class="token operator">=</span> temp<span class="token punctuation">;</span>

                flag <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
            <span class="token punctuation">}</span>
        <span class="token punctuation">}</span>

        <span class="token keyword">if</span> <span class="token punctuation">(</span>flag <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">)</span>
            <span class="token keyword">break</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h2 id="insertion-sort" tabindex="-1"><a class="header-anchor" href="#insertion-sort" aria-hidden="true">#</a> Insertion Sort</h2><p>Insertion Sort is a simple sorting algorithm that builds the final sorted array one element at a time. It&#39;s based on the idea of iteratively inserting elements from the unsorted portion of the array into their correct positions within the sorted portion. This process continues until all elements have been inserted and the entire array becomes sorted</p><p><strong>Steps</strong>:</p><ol><li><p>Initial State: The algorithm considers the first element as the initially sorted portion and the remaining elements as the unsorted portion</p></li><li><p>Iterative Insertion: For each element in the unsorted portion, the algorithm compares it to the elements in the sorted portion from right to left. It finds the correct position for the element within the sorted portion by shifting larger elements to the right</p></li><li><p>Insertion: Once the correct position is found, the element is inserted into its place within the sorted portion</p></li><li><p>Repeat: Steps 2 and 3 are repeated for all remaining elements in the unsorted portion until all elements have been inserted into the sorted portion</p></li></ol><p><em>Pseudocode</em>:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>function insertionSort(arr):
    n = length(arr)
    for i from 1 to n-1:
        currentElement = arr[i]
        j = i - 1
        while j &gt;= 0 and arr[j] &gt; currentElement:
            arr[j + 1] = arr[j]
            j = j - 1
        arr[j + 1] = currentElement
</code></pre></div><p><em>Example:</em></p><p>Let&#39;s say you have an unsorted list: <code>[5, 2, 9, 1, 5]</code></p><ol><li>Pass 1: <code>[2, 5, 9, 1, 5]</code></li><li>Pass 2: <code>[2, 5, 9, 1, 5]</code></li><li>Pass 3: <code>[1, 2, 5, 9, 5]</code></li><li>Pass 4: <code>[1, 2, 5, 5, 9]</code></li></ol><p>For an array of size <code>n</code> at worst case:</p><ul><li>Number of Passes: <code>n-1</code></li><li>Max Number of Comparisons: <code>(n * (n - 1)) / 2</code> --&gt; <code>O(n^2)</code></li><li>Max Number of Swaps: <code>(n * (n - 1)) / 2</code> --&gt; <code>O(n^2)</code></li></ul><p>If the list is already sorted:</p><ul><li>Max Number of Comparisons: <code>n - 1</code> --&gt; <code>O(n)</code></li><li>Max Number of Swaps: <code>0</code> --&gt; <code>O(1)</code></li></ul><table><thead><tr><th></th><th style="text-align:right;">Bubble Sort</th><th style="text-align:right;">Insertion Sort</th><th style="text-align:right;">Cases</th></tr></thead><tbody><tr><td>Min Comparison</td><td style="text-align:right;"><code>O(n)</code></td><td style="text-align:right;"><code>O(n)</code></td><td style="text-align:right;">Already Sorted</td></tr><tr><td>Max Comparison</td><td style="text-align:right;"><code>O(n^2)</code></td><td style="text-align:right;"><code>O(n^2)</code></td><td style="text-align:right;">Sorted in Reverse</td></tr><tr><td>Min Swap</td><td style="text-align:right;"><code>O(1)</code></td><td style="text-align:right;"><code>O(1)</code></td><td style="text-align:right;">Already Sorted</td></tr><tr><td>Max Swap</td><td style="text-align:right;"><code>O(n^2)</code></td><td style="text-align:right;"><code>O(n^2)</code></td><td style="text-align:right;">Sorted in Reverse</td></tr><tr><td>Adaptive</td><td style="text-align:right;">Yes</td><td style="text-align:right;">Yes</td><td style="text-align:right;"></td></tr><tr><td>Stable</td><td style="text-align:right;">Yes</td><td style="text-align:right;">Yes</td><td style="text-align:right;"></td></tr><tr><td>Linked List</td><td style="text-align:right;">-</td><td style="text-align:right;">Yes</td><td style="text-align:right;"></td></tr><tr><td>k Passes</td><td style="text-align:right;">Yes</td><td style="text-align:right;">No</td><td style="text-align:right;"></td></tr></tbody></table><ul><li><p>Insertion Sort is particularly efficient when dealing with small arrays or partially sorted arrays, as the number of comparisons and shifts is relatively small in those cases</p></li><li><p>It&#39;s an in-place sorting algorithm, meaning it doesn&#39;t require additional memory space proportional to the input size for sorting</p></li><li><p>Insertion is better for Linked List than Arrays</p></li><li><p>By <strong>nature it is Adaptive</strong></p></li><li><p>It is <strong>Stable</strong></p></li></ul><h2 id="selection-sort" tabindex="-1"><a class="header-anchor" href="#selection-sort" aria-hidden="true">#</a> Selection Sort</h2><p>Selection sort is an <em>in-place comparison-based algorithm</em> in which the list is divided into two parts, the sorted part at the left end and the unsorted part at the right end. Initially, the sorted part is empty and the unsorted part is the entire list</p><p>The smallest element is selected from the unsorted array and swapped with the leftmost element, and that element becomes a part of the sorted array. This process continues moving unsorted array boundary by one element to the right</p><p><strong>Steps</strong>:</p><ol><li><p>Initial State: The algorithm starts with the entire array being unsorted and an empty sorted portion</p></li><li><p>Find the Minimum: In each iteration, the algorithm scans the unsorted portion of the array to find the minimum (or maximum) element</p></li><li><p>Swap: Once the minimum (or maximum) element is found, it&#39;s swapped with the leftmost element in the unsorted portion. This effectively moves the selected element to the end of the sorted portion</p></li><li><p>Expand Sorted Portion: After the swap, the sorted portion grows by one element, and the unsorted portion shrinks by one element</p></li><li><p>Repeat: Steps 2 to 4 are repeated until the entire array is sorted. The sorted portion gradually expands while the unsorted portion shrinks until there are no more elements left in the unsorted portion</p></li><li><p>Final State: At the end of the process, the array is fully sorted</p></li></ol><p><em>Pseudocode</em>:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>function selectionSort(arr):
    n = length(arr)
    for i from 0 to n-2:  // The last element will be in the correct position after n-1 iterations
        minIndex = i
        for j from i+1 to n-1:
            if arr[j] &lt; arr[minIndex]:
                minIndex = j
        if minIndex != i:
            swap(arr[i], arr[minIndex])
</code></pre></div><p><em>Example:</em></p><p>Let&#39;s say you have an unsorted list: <code>[5, 2, 9, 1, 5]</code></p><ol><li>Pass 1: Find the smallest, which is <code>1</code> and swap with left most <code>5</code>: <code>[1, 2, 9, 5, 5]</code></li><li>Pass 2: <code>[1, 2, 9, 5, 5]</code></li><li>Pass 3: <code>[1, 2, 5, 9, 5]</code></li><li>Pass 4: <code>[1, 2, 5, 5, 9]</code></li><li>Pass 5: <code>[1, 2, 5, 5, 9]</code></li></ol><ul><li>Each element is compared in every operation, with number of operations same as the number of elements. With each operation an element is moved to the new array, so one less element to be compared in the next operation. On an average, (1/2)n elements are compared for each operation. Thus, the runtime is <strong>O(n x (1/2)n)</strong>. But constants like <em>(1/2)</em> are ignored in Big O. So the runtime is written as <em>O(n^2)</em>.</li></ul><p>For an array of size <code>n</code> at worst case:</p><ul><li><p>Number of Passes: <code>n-1</code></p></li><li><p>Max Number of Comparisons: <code>(n * (n - 1)) / 2</code> --&gt; <code>O(n^2)</code></p></li><li><p>Max Number of Swaps: <code>n-1</code> --&gt; <code>O(n)</code></p></li><li><p>It is <strong>not Stable</strong>: does not guarantee stability. When selecting the minimum (or maximum) element to be placed in its correct position, Selection Sort doesn&#39;t consider the relative order of equal elements. As a result, if there are equal elements in the array, their order might change during the sorting process</p></li><li><p>Is is <strong>not Adaptive</strong>: Its time complexity remains the same regardless of the input&#39;s initial order. It doesn&#39;t take advantage of any pre-existing order in the data. It always scans the entire unsorted portion of the array to find the minimum (or maximum) element, regardless of whether the array is partially sorted or not</p></li><li><p>We get <strong>smallest (or largest if desc) number after just 1 pass</strong> (the first element)</p></li></ul><p><em>Implementations:</em></p><div class="language-python" data-ext="py"><pre class="language-python"><code><span class="token keyword">def</span> <span class="token function">find_smallest</span><span class="token punctuation">(</span>arr<span class="token punctuation">)</span><span class="token punctuation">:</span>
    smallest <span class="token operator">=</span> arr<span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span>
    smallest_index <span class="token operator">=</span> <span class="token number">0</span>
    <span class="token keyword">for</span> i <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token builtin">len</span><span class="token punctuation">(</span>arr<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">:</span>
        <span class="token keyword">if</span> arr<span class="token punctuation">[</span>i<span class="token punctuation">]</span> <span class="token operator">&lt;</span> smallest<span class="token punctuation">:</span>
            smallest <span class="token operator">=</span> arr<span class="token punctuation">[</span>i<span class="token punctuation">]</span>
            smallest_index <span class="token operator">=</span> i
    <span class="token keyword">return</span> smallest_index

<span class="token comment"># Selection Sort</span>
<span class="token keyword">def</span> <span class="token function">selection_sort</span><span class="token punctuation">(</span>arr<span class="token punctuation">)</span><span class="token punctuation">:</span>
    newArr <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>
    <span class="token keyword">for</span> i <span class="token keyword">in</span> <span class="token builtin">range</span><span class="token punctuation">(</span><span class="token builtin">len</span><span class="token punctuation">(</span>arr<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">:</span>
        smallest <span class="token operator">=</span> find_smallest<span class="token punctuation">(</span>arr<span class="token punctuation">)</span>
        newArr<span class="token punctuation">.</span>append<span class="token punctuation">(</span>arr<span class="token punctuation">.</span>pop<span class="token punctuation">(</span>smallest<span class="token punctuation">)</span><span class="token punctuation">)</span>
    <span class="token keyword">return</span> newArr
</code></pre></div><h2 id="quick-sort" tabindex="-1"><a class="header-anchor" href="#quick-sort" aria-hidden="true">#</a> Quick Sort</h2>`,55),u=t("li",null,[t("p",null,"Partitioning procedure")],-1),h=t("li",null,[t("p",null,"Not suited for sorted list or reverse sorted list")],-1),m=t("li",null,[t("p",null,[t("em",null,"Inductive proofs")])],-1),g=t("li",null,[t("p",null,"Randomized Quick Sort")],-1),f=t("li",null,[t("p",null,"Selection Exchange Sort")],-1),k=t("li",null,[t("p",null,"Partition Exchange Sort")],-1),y=t("h2",{id:"merge-sort",tabindex:"-1"},[t("a",{class:"header-anchor",href:"#merge-sort","aria-hidden":"true"},"#"),e(" Merge Sort")],-1),b=o(`<p><strong>Steps</strong>:</p><ol><li><p>Divide: The original array is recursively divided into smaller sub-arrays until each sub-array contains only one element or is empty</p></li><li><p>Conquer: The individual elements or small sub-arrays are already sorted by definition. This is the base case of the recursion</p></li><li><p>Merge: The sorted sub-arrays are then merged back together in a way that maintains their order. This merging process combines two sorted arrays into a single sorted array</p></li></ol><p><em>Pseudocode</em>:</p><div class="language-text" data-ext="text"><pre class="language-text"><code>function mergeSort(arr):
    if length(arr) &lt;= 1:
        return arr

    mid = length(arr) / 2

    // Divide the array into left and right sub-arrays
    left = arr[0:mid]
    right = arr[mid:end]

    // Recursively sort the left and right sub-arrays
    left = mergeSort(left)
    right = mergeSort(right)

    // Merge the sorted sub-arrays
    sortedArray = merge(left, right)
    return sortedArray

function merge(left, right):
    result = []
    leftIndex = 0
    rightIndex = 0

    while leftIndex &lt; length(left) and rightIndex &lt; length(right):
        if left[leftIndex] &lt;= right[rightIndex]:
            result.append(left[leftIndex])
            leftIndex++
        else:
            result.append(right[rightIndex])
            rightIndex++

    // Add remaining elements from both arrays
    while leftIndex &lt; length(left):
        result.append(left[leftIndex])
        leftIndex++

    while rightIndex &lt; length(right):
        result.append(right[rightIndex])
        rightIndex++

    return result
</code></pre></div><p>Time complexity:</p><ul><li>Best case: <strong><code>O(n log n)</code></strong></li><li>Worst case: <strong><code>O(n log n)</code></strong></li></ul><p>Merging 2 Sorted Arrays to create a new sorted array</p><ul><li><p><code>O(n log n)</code></p></li><li><p>It is <strong>Stable</strong>: During the merging step, when two elements are compared and inserted into the merged array, if they are equal, the element from the left sub-array is inserted before the element from the right sub-array. This maintains the relative order of equal elements, making Merge Sort a stable sorting algorithm</p></li><li><p>It is <strong>Adaptive</strong>: Although its basic structure doesn&#39;t inherently take advantage of partially sorted data, its time complexity remains efficient even when dealing with partially sorted arrays. The reason is that Merge Sort always divides the array into smaller sub-arrays and eventually merges them. This merging process efficiently combines even small sorted sub-arrays into larger sorted sub-arrays. While not as inherently adaptive as algorithms like Insertion Sort, Merge Sort still performs well with partially sorted data due to its divide-and-conquer nature</p></li></ul><h2 id="count-sort" tabindex="-1"><a class="header-anchor" href="#count-sort" aria-hidden="true">#</a> Count Sort</h2><ul><li>Index based sorting</li></ul><h2 id="bucket-bin-sort" tabindex="-1"><a class="header-anchor" href="#bucket-bin-sort" aria-hidden="true">#</a> Bucket/Bin Sort</h2><h2 id="shell-sort" tabindex="-1"><a class="header-anchor" href="#shell-sort" aria-hidden="true">#</a> Shell Sort</h2><p>Uses <a href="#insertion-sort">Insertion Sort</a></p><h2 id="overview" tabindex="-1"><a class="header-anchor" href="#overview" aria-hidden="true">#</a> Overview</h2><p>Here&#39;s a comparison of the average-case time complexities of various sorting algorithms:</p><table><thead><tr><th>Algorithm</th><th>Space Complexity</th><th>Time Complexity (Worst)</th><th>Best</th><th>Avgerage</th><th>Adaptive</th><th>Stable</th></tr></thead><tbody><tr><td>Bubble Sort</td><td><code>O(1)</code></td><td></td><td></td><td><code>O(n^2)</code></td><td>Yes</td><td>Yes</td></tr><tr><td>Selection Sort</td><td><code>O(1)</code></td><td></td><td></td><td><code>O(n^2)</code></td><td>No</td><td>No</td></tr><tr><td>Insertion Sort</td><td><code>O(1)</code></td><td></td><td></td><td><code>O(n^2)</code></td><td>Yes</td><td>Yes</td></tr><tr><td>Merge Sort</td><td><code>O(n)</code></td><td></td><td></td><td><code>O(n log n)</code></td><td>Yes</td><td>Yes</td></tr><tr><td>Quick Sort</td><td><code>O(log n)</code></td><td></td><td></td><td><code>O(n log n)</code></td><td>No</td><td>No (can be made)</td></tr><tr><td>Heap Sort</td><td><code>O(1)</code></td><td></td><td></td><td><code>O(n log n)</code></td><td>No</td><td>No</td></tr><tr><td>Counting Sort</td><td><code>O(k)</code></td><td></td><td></td><td><code>O(n + k)</code> (<code>k</code> = range of input values)</td><td>No</td><td>Yes</td></tr><tr><td>Radix Sort</td><td><code>O(n + k)</code></td><td></td><td></td><td><code>O(nk)</code> (<code>k</code> = number of digits in the maximum number)</td><td>No</td><td>Yes</td></tr><tr><td>Bucket Sort</td><td><code>O(n + k)</code></td><td></td><td><code>O(n)</code> (uniform distribution)</td><td><code>O(n^2)</code></td><td>No</td><td>Yes</td></tr></tbody></table><p>Space Complexity:</p><ul><li>All <code>O(1)</code> are In-place algorithm, hence no additional space is required</li><li>Merge sort: Requires additional space for merging sub-arrays, not in-place</li><li>Quick Sort: Recursive algorithm, requires additional space for the call stack</li><li>Counting Sort: Requires additional space for counting array, where <code>k</code> is the range of input values</li><li>Radix Sort: Requires additional space for intermediate arrays, where <code>k</code> is the number of digits in the maximum number</li><li>Bucket Sort: Requires additional space for buckets and intermediate arrays, can be high for small ranges</li></ul>`,18);function x(w,S){const n=i("RouterLink");return l(),d("div",null,[c,t("ul",null,[t("li",null,[t("p",null,[e("It uses "),s(n,{to:"/Concepts/Data-Structures_and_Algorithms/Algorithms/Algorithms.html#divide--conquer"},{default:a(()=>[e("Divide & Conquer")]),_:1}),e(" technique")])]),u,h,m,g,f,k]),y,t("p",null,[e("Merge Sort is a popular comparison-based sorting algorithm that follows the "),s(n,{to:"/Concepts/Data-Structures_and_Algorithms/Algorithms/Algorithms.html#divide--conquer"},{default:a(()=>[e("Divide & Conquer")]),_:1}),e(" strategy to efficiently sort an array or list of elements. It works by recursively dividing the array into smaller sub-arrays, sorting those sub-arrays, and then merging them back together to create a sorted final array")]),b])}const I=r(p,[["render",x],["__file","Sorting_Algorithms.html.vue"]]);export{I as default};
