import{_ as t,r as o,o as i,c as l,b as e,d as s,a as n,e as c}from"./app-jDLxG9B3.js";const r={},p=e("h1",{id:"shell-scripts",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#shell-scripts","aria-hidden":"true"},"#"),s(" Shell Scripts")],-1),d=e("p",null,"Scripts contain a series of commands. An interpreter executes these commands.",-1),h=e("p",null,"Great for automating repetitive tasks",-1),u={href:"https://explainshell.com/",target:"_blank",rel:"noopener noreferrer"},m={href:"https://www.shellcheck.net/",target:"_blank",rel:"noopener noreferrer"},k=c(`<div class="custom-container tip"><p class="custom-container-title">Note</p><p>Anything you can type at the command line, you can put in a script.</p></div><h2 id="bash" tabindex="-1"><a class="header-anchor" href="#bash" aria-hidden="true">#</a> Bash</h2><p>GNU Bash is a Unix shell. Bash is a command processor. Bash can read and execute commands from a file, called a shell script.</p><h2 id="first-shell-script" tabindex="-1"><a class="header-anchor" href="#first-shell-script" aria-hidden="true">#</a> First Shell Script</h2><p>Let&#39;s create a simple script, that prints <code>Hello World</code> to the terminal.</p><ol><li><p>Create a <code>script.sh</code> file with the below content:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token shebang important">#!/bin/bash</span>
<span class="token builtin class-name">echo</span> <span class="token string">&quot;Hello World&quot;</span>
</code></pre></div></li><li><p>To execute this script, just enter the file name in the command line, like this <code>./script.sh</code>.</p></li><li><p>If you get any file permission errors, try providing the file executable permission: <code>chmod 755 script.sh</code></p></li><li><p>Output:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>Hello World
</code></pre></div></li></ol><h2 id="use-of-shebang" tabindex="-1"><a class="header-anchor" href="#use-of-shebang" aria-hidden="true">#</a> Use of Shebang</h2><p>It is used to specify the shell program that needs to be used to execute the script file</p><p>When the interpreter executes the script. The first line, i.e. shebang is used as an argument for the path of the shell program. This can be observed below:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># run the script in background</span>
./script.sh <span class="token operator">&amp;</span>

<span class="token comment"># check the process</span>
<span class="token function">ps</span> <span class="token parameter variable">-ef</span> process_id

<span class="token comment"># check commands executed by script.sh</span>
pstree <span class="token parameter variable">-p</span> process_id
</code></pre></div><p>Different Bash shebang:</p><ul><li><p><code>#!/usr/bin/env bash</code>: Flexibility on different systems (protability)</p><ul><li>Different <code>*nixes</code> put <code>bash</code> in different places</li></ul></li><li><p><code>#!/usr/bin/bash</code>: Explicit control on a given system of what executable is called</p></li></ul><div class="custom-container warning"><p class="custom-container-title">Why Shebang is needed?</p><ul><li>If no shebang is specified, then your default shell will be used.</li><li>Different shells have slightly varying syntax and this may cause errors.</li></ul></div><div class="custom-container tip"><p class="custom-container-title">Other Interpreters</p><p>Use python interpreter path as shebang if executing python script or any other interpreter path for that matter.</p></div><h2 id="comments" tabindex="-1"><a class="header-anchor" href="#comments" aria-hidden="true">#</a> Comments</h2><p>Pound sign (<code>#</code>) is used for writing in-line comments.</p><h2 id="variables" tabindex="-1"><a class="header-anchor" href="#variables" aria-hidden="true">#</a> Variables</h2><p>Storage locations that have a name.</p><h3 id="declaring-variables" tabindex="-1"><a class="header-anchor" href="#declaring-variables" aria-hidden="true">#</a> Declaring Variables</h3><p>Syntax: <code>VARIABLE_NAME=&quot;Value&quot;</code></p><p>Do not use spaces before or after <strong>=</strong> sign.</p><p>You can assign command output to a variable, by enclosing the command inside <code>( )</code> braces like: <code>USER_NAME=$(who)</code></p><blockquote><p>By convention variables are written in uppercase.</p></blockquote><div class="custom-container tip"><p class="custom-container-title">Note</p><p>You can use back ticks or Tilda instead of the <code>$( )</code> syntax. This is an older convention.</p></div><h3 id="using-variables" tabindex="-1"><a class="header-anchor" href="#using-variables" aria-hidden="true">#</a> Using Variables</h3><p>There are two ways to use them:</p><ol><li><p>Prefix dollar (<code>$</code>) sign to the variable:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token assign-left variable">MY_SHELL</span><span class="token operator">=</span><span class="token string">&quot;BASH&quot;</span>
<span class="token builtin class-name">echo</span> <span class="token string">&quot;I like the <span class="token variable">$MY_SHELL</span> shell.&quot;</span>
</code></pre></div></li><li><p>Enclose the variable inside curly <code>{ }</code> braces and prefix <code>$</code> sign. This is used whenever you need to concatenate the variable with some other text:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">echo</span> <span class="token string">&quot;I like <span class="token variable">\${MY_SHELL}</span>ing script&quot;</span>
</code></pre></div></li></ol><h2 id="tests" tabindex="-1"><a class="header-anchor" href="#tests" aria-hidden="true">#</a> Tests</h2><p>It checks for a condition, if the condition is met then it proceeds to execute the code enclosed within its block. Else it will skip that block and proceed with the next statements.</p><p>Syntax: <code>[ condition-to-test-for ]</code></p><p>To check the different test operators use <code>help test</code> or <code>man test</code>.</p><h3 id="some-file-operators-tests" tabindex="-1"><a class="header-anchor" href="#some-file-operators-tests" aria-hidden="true">#</a> Some File operators (tests)</h3><ul><li><code>-d FILE</code> : True if file is a directory.</li><li><code>-e FILE</code> : True if file exists.</li><li><code>-f FILE</code> : True if file exists and is a regular file.</li><li><code>-r FILE</code> : True if file is readable by you.</li><li><code>-s FILE</code> : True if file exists and is not empty.</li><li><code>-w FILE</code> : True if the file is writable by you.</li><li><code>-x FILE</code> : True if file is executable by you.</li></ul><h3 id="string-operators-tests" tabindex="-1"><a class="header-anchor" href="#string-operators-tests" aria-hidden="true">#</a> String operators (tests)</h3><ul><li><code>-z STRING</code> : True if string is empty.</li><li><code>-n STRING</code> : True if string is not empty.</li><li><code>STRING1=STRING2</code> : True if the strings are equal.</li><li><code>STRING1!=STRING2</code> : True if the strings are not equal.</li></ul><h3 id="arithmetic-operators-tests" tabindex="-1"><a class="header-anchor" href="#arithmetic-operators-tests" aria-hidden="true">#</a> Arithmetic Operators (tests)</h3><ul><li><code>ARG1 -eq AEG2</code> : True I both are equal.</li><li><code>-ne</code> : not equal.</li><li><code>-lt</code> : less than</li><li><code>-le</code> : less than or equal to</li><li><code>-gt</code> : greater than</li><li><code>-ge</code> : greater than or equal to</li></ul><h2 id="if-else-statement" tabindex="-1"><a class="header-anchor" href="#if-else-statement" aria-hidden="true">#</a> if-else statement</h2><p>Syntax for <code>if</code>:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token keyword">if</span> <span class="token punctuation">[</span> condition-is-true <span class="token punctuation">]</span>
<span class="token keyword">then</span>
  <span class="token builtin class-name">command</span> <span class="token number">1</span>
  <span class="token builtin class-name">command</span> <span class="token number">2</span>
  <span class="token builtin class-name">command</span> n
<span class="token keyword">fi</span>

<span class="token comment"># double bracket [[</span>
<span class="token comment"># this construct is not POSIX compliant</span>
<span class="token comment"># and only available in Bash shell along with a few other shells</span>
<span class="token keyword">if</span> <span class="token punctuation">[</span><span class="token punctuation">[</span> <span class="token variable">$MY_SHELL</span> <span class="token operator">==</span> <span class="token string">&quot;bash&quot;</span> <span class="token punctuation">]</span><span class="token punctuation">]</span>
</code></pre></div><p>Example:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token keyword">if</span> <span class="token punctuation">[</span> <span class="token string">&quot;<span class="token variable">$MY_SHELL</span>&quot;</span><span class="token operator">=</span><span class="token string">&quot;bash&quot;</span> <span class="token punctuation">]</span>
<span class="token keyword">then</span>
  <span class="token builtin class-name">echo</span> <span class="token string">&quot;You seem to like bash shell.&quot;</span>
<span class="token keyword">elif</span> <span class="token punctuation">[</span> <span class="token string">&quot;<span class="token variable">$MY_SHELL</span>&quot;</span><span class="token operator">=</span><span class="token string">&quot;ksh&quot;</span> <span class="token punctuation">]</span>
<span class="token keyword">then</span>
  <span class="token builtin class-name">echo</span> <span class="token string">&quot;You seem to like ksh Shell.&quot;</span>
<span class="token keyword">else</span>
  <span class="token builtin class-name">echo</span> <span class="token string">&quot;You don&#39;t seem to like the bash shell.&quot;</span>
<span class="token keyword">fi</span>
</code></pre></div><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>Use variables under quotes to prevent unknown errors</p></div><h2 id="for-loop" tabindex="-1"><a class="header-anchor" href="#for-loop" aria-hidden="true">#</a> for Loop</h2><p>Syntax:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token keyword">for</span> <span class="token for-or-select variable">VARIABLE_NAME</span> <span class="token keyword">in</span> ITEM_1 ITEM_2 ITEM_N
<span class="token keyword">do</span>
  <span class="token builtin class-name">command</span> n
<span class="token keyword">done</span>
</code></pre></div><p>Example:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token keyword">for</span> <span class="token for-or-select variable">COLOR</span> <span class="token keyword">in</span> red green blue
<span class="token keyword">do</span>
  <span class="token builtin class-name">echo</span> <span class="token string">&quot;COLOR: \\<span class="token variable">$COLOR</span>&quot;</span>
<span class="token keyword">done</span>
</code></pre></div><ul><li><p>The list is usually stored inside a variable. With white space separating each item.</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token assign-left variable">COLORS</span><span class="token operator">=</span><span class="token string">&quot;red green blue&quot;</span>

<span class="token keyword">for</span> <span class="token for-or-select variable">COLOR</span> <span class="token keyword">in</span> <span class="token variable">$COLORS</span>
  <span class="token keyword">do</span>
    <span class="token builtin class-name">echo</span> <span class="token string">&quot;COLOR: <span class="token variable">$COLOR</span>&quot;</span>
<span class="token keyword">done</span>
</code></pre></div></li></ul><h2 id="positional-parameters" tabindex="-1"><a class="header-anchor" href="#positional-parameters" aria-hidden="true">#</a> Positional Parameters</h2><p>From <code>$0</code>-<code>$9</code> are used as positional parameters.</p><ul><li><code>$0</code> : Name of the script being run.</li><li>Rest are parameters passed through command line.</li><li><code>$@</code> is the list of all arguments. It can be used to loop through the parameters.</li></ul><h2 id="user-input-stdin" tabindex="-1"><a class="header-anchor" href="#user-input-stdin" aria-hidden="true">#</a> User Input (STDIN)</h2><p>The <code>read</code> command accepts STDIN.</p><p>Syntax:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">read</span> <span class="token parameter variable">-p</span> <span class="token string">&quot;PROMPT&quot;</span> VARIABLE
</code></pre></div><h2 id="output" tabindex="-1"><a class="header-anchor" href="#output" aria-hidden="true">#</a> Output</h2><p>File descriptors:</p><ul><li><code>0</code> is <code>stdin</code> the <em>standard input</em></li><li><code>1</code> is <code>stdout</code> the <em>standard output</em></li><li><code>2</code> is <code>stderr</code> the <em>standard error</em></li></ul><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># redirect stdout to file.txt</span>
<span class="token builtin class-name">echo</span> <span class="token builtin class-name">test</span> <span class="token operator">&gt;</span> file.txt
<span class="token builtin class-name">echo</span> <span class="token builtin class-name">test</span> <span class="token operator"><span class="token file-descriptor important">1</span>&gt;</span> file.txt

<span class="token comment"># redirect stderr to file.txt</span>
<span class="token builtin class-name">echo</span> <span class="token builtin class-name">test</span> <span class="token operator"><span class="token file-descriptor important">2</span>&gt;</span> file.txt

<span class="token comment"># redirect stderr and stdout to file.txt</span>
<span class="token builtin class-name">echo</span> <span class="token builtin class-name">test</span> <span class="token operator"><span class="token file-descriptor important">2</span>&gt;</span><span class="token file-descriptor important">&amp;1</span> file.txt
</code></pre></div><ul><li><p><code>&gt;</code>: <em>redirection</em></p></li><li><p><code>&gt;&gt;</code>: append to target if exists</p></li><li><p><code>&gt;&amp;</code>: redirect a <em>stream</em> to another <em>file descriptor</em></p></li></ul><p>Thus, in <code>2&gt;&amp;1</code>:</p><ul><li><code>2&gt;</code> redirects <code>stderr</code> to an <em>(unspecified) file</em></li><li><code>&amp;1</code> redirects <code>stderr</code> to <code>stdout</code></li></ul>`,63);function b(g,f){const a=o("ExternalLinkIcon");return i(),l("div",null,[p,d,h,e("ul",null,[e("li",null,[e("a",u,[s("Explain Shell Commands"),n(a)])]),e("li",null,[e("a",m,[s("ShellCheck: Finds bugs in your shell scripts"),n(a)])])]),k])}const x=t(r,[["render",b],["__file","Bash.html.vue"]]);export{x as default};
