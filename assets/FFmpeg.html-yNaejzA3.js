import{_ as r,r as o,o as p,c as l,b as a,d as e,a as n,e as t}from"./app-jDLxG9B3.js";const c={},i=t(`<h1 id="ffmpeg" tabindex="-1"><a class="header-anchor" href="#ffmpeg" aria-hidden="true">#</a> FFmpeg</h1><p>FFmpeg is a free and open-source software project consisting of a suite of libraries and programs for handling video, audio, and other multimedia files and streams.</p><p>Check streams:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>ffprobe <span class="token parameter variable">-v</span> error <span class="token parameter variable">-show_entries</span> <span class="token assign-left variable">stream</span><span class="token operator">=</span>index,codec_name,codec_type movie.mkv
</code></pre></div><p>Combine audio and video:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>ffmpeg <span class="token parameter variable">-i</span> video.mkv <span class="token parameter variable">-i</span> audio.mp3 <span class="token parameter variable">-map</span> <span class="token number">0</span>:v <span class="token parameter variable">-map</span> <span class="token number">1</span>:a <span class="token parameter variable">-c:v</span> copy <span class="token parameter variable">-shortest</span> movie.mkv
</code></pre></div><p>Audio:</p><ul><li><code>mp3</code>: 128, 144, 160, <em>192</em></li><li><code>vorbis</code>: 128, 144, <em>160</em>, 192 (<code>ogg</code>)</li><li><code>aac</code>: 128, 144, 160, 192 (<code>m4a</code>)</li><li><code>opus</code>: 128, <em>144</em>, 160, 192 (<code>ogg</code>/<code>opus</code>)</li></ul><h2 id="audio" tabindex="-1"><a class="header-anchor" href="#audio" aria-hidden="true">#</a> Audio</h2><p>Audio conversion:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>ffmpeg <span class="token parameter variable">-i</span> audio.mka <span class="token parameter variable">-acodec</span> libmp3lame audio.mp3
</code></pre></div><p>Extract audio:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># -map channel</span>
ffmpeg <span class="token parameter variable">-i</span> movie.mkv <span class="token parameter variable">-vn</span> <span class="token parameter variable">-map</span> <span class="token number">0</span>:4 <span class="token parameter variable">-acodec</span> copy output.mka
</code></pre></div><h2 id="video" tabindex="-1"><a class="header-anchor" href="#video" aria-hidden="true">#</a> Video</h2><p>Extract video:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>ffmpeg <span class="token parameter variable">-i</span> movie.mkv <span class="token parameter variable">-an</span> <span class="token parameter variable">-map</span> <span class="token number">0</span>:0 <span class="token parameter variable">-vcodec</span> copy video.mkv
</code></pre></div><h2 id="x265" tabindex="-1"><a class="header-anchor" href="#x265" aria-hidden="true">#</a> x265</h2>`,17),d={href:"https://x265.readthedocs.io/en/master/",target:"_blank",rel:"noopener noreferrer"},h=t(`<p>An application for encoding video streams into the H.265/MPEG-H HEVC compression format.</p><h3 id="basic-commands" tabindex="-1"><a class="header-anchor" href="#basic-commands" aria-hidden="true">#</a> Basic Commands</h3><ol><li><p>Simple encoding:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>x265 inputFile.y4u outFile.hevc
</code></pre></div><blockquote><p>Only <em><code>yuv</code></em> or <em><code>y4m</code></em> can be used as input!</p></blockquote></li><li><p>Encoding Statistics:</p><ul><li><p>CLI log:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>--log-level <span class="token operator">&lt;</span>integer<span class="token operator">|</span>string<span class="token operator">&gt;</span>
</code></pre></div><p><em>where</em></p><table><thead><tr><th>value</th><th>log-level</th></tr></thead><tbody><tr><td>0</td><td>error</td></tr><tr><td>1</td><td>warning</td></tr><tr><td>2</td><td>info (default)</td></tr><tr><td>3</td><td>debug</td></tr><tr><td>4</td><td>full</td></tr><tr><td>-1</td><td>disable</td></tr></tbody></table></li><li><p>Statistics output to file :</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token parameter variable">--csv</span> <span class="token operator">&lt;</span>filename<span class="token operator">&gt;</span>
</code></pre></div><ul><li>If <strong><code>--csv-log-level</code></strong> is <code>0</code> then <em>one line per run</em>.</li><li>If <em>greater</em> than <code>0</code> then <em>one line per frame</em>.</li></ul></li></ul></li><li><p>Progress:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>--no-progress
</code></pre></div><blockquote><p>Disable periodic progress reports from the CLI</p></blockquote></li></ol><h2 id="audio-and-video-recording" tabindex="-1"><a class="header-anchor" href="#audio-and-video-recording" aria-hidden="true">#</a> Audio and Video Recording</h2><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>ffmpeg <span class="token parameter variable">-f</span> alsa <span class="token parameter variable">-ac</span> <span class="token number">2</span> <span class="token parameter variable">-i</span> default <span class="token parameter variable">-acodec</span> libopus <span class="token parameter variable">-threads</span> <span class="token number">0</span> <span class="token parameter variable">-y</span> output.mka
</code></pre></div><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>ffmpeg <span class="token parameter variable">-f</span> x11grab <span class="token parameter variable">-r</span> <span class="token number">20</span> <span class="token parameter variable">-s</span> 1920x1080 <span class="token parameter variable">-i</span> :0.0 <span class="token parameter variable">-vcodec</span> libx265 <span class="token parameter variable">-preset</span> fast <span class="token parameter variable">-b:v</span> 500k <span class="token parameter variable">-threads</span> <span class="token number">0</span> <span class="token parameter variable">-y</span> output.6.mkv
</code></pre></div><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>ffmpeg <span class="token parameter variable">-f</span> pulse <span class="token parameter variable">-ac</span> <span class="token number">2</span> <span class="token parameter variable">-i</span> default <span class="token parameter variable">-f</span> x11grab <span class="token parameter variable">-r</span> <span class="token number">20</span> <span class="token parameter variable">-s</span> 1920x1080 <span class="token parameter variable">-i</span> :0.0 <span class="token parameter variable">-acodec</span> aac <span class="token parameter variable">-vcodec</span> libx264 <span class="token parameter variable">-preset</span> ultrafast <span class="token parameter variable">-b:v</span> 900k <span class="token parameter variable">-threads</span> <span class="token number">0</span> <span class="token parameter variable">-y</span> output.mkv
</code></pre></div><h2 id="something" tabindex="-1"><a class="header-anchor" href="#something" aria-hidden="true">#</a> Something</h2><p>I have png images from <code>0000.png</code> through <code>0120.png</code>. I want to use ffmpeg to convert them into an <code>mp4</code> video. The images have an alpha channel, I would like the alpha channel to be composited to the right side of the rgb channels, so that the video has double the width of the input images, with the rgb on the left half and the alpha on the right half. The alpha should be represented as black for 100% transparent and white for 0% transparent</p><p>Here is an ffmpeg command that you can use to convert a sequence of png images with an alpha channel into an mp4 video:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>ffmpeg <span class="token parameter variable">-framerate</span> <span class="token number">24</span> <span class="token parameter variable">-i</span> %04d.png <span class="token parameter variable">-vf</span> <span class="token string">&quot;split[rgb][a]:[rgb]setpts=PTS-STARTPTS[rgb]:[a]alphaextract.format=gray[alpha]:[rgb][alpha]hstack&quot;</span> <span class="token parameter variable">-c:v</span> 1ibx264 <span class="token parameter variable">-crf</span> <span class="token number">23</span> <span class="token parameter variable">-preset</span> veryfast output.mp4
</code></pre></div><p>This command will convert the png images <code>0000. png</code> through <code>0120.png</code> at a frame rate of 24 frames per second. The <code>-vf</code> option specifies the video filterchain that is used to process the images. In this case, the filterchain uses the <code>split</code> filter to split the input images into their red, green, blue (RGB) and alpha channels. The <code>setpts</code> filter is then used to adjust the timing of the RGB channels so that they match the alpha channels. The <code>alphaextract</code> filter is used to extract the alpha channel and convert it to grayscale. The <code>format</code> filter is then used to convert the alpha channel to the desired format (in this case, grayscale). Finally, the <code>hstack</code> filter is used to stack the RGB and alpha channels horizontally, with the RGB channels on the left and the alpha channel on the right. The <code>-c:v</code> option specifies the codec that should be used for the video, and the <code>-crf</code> and <code>-preset</code> options control the quality and encoding speed of the output video</p><h2 id="tools" tabindex="-1"><a class="header-anchor" href="#tools" aria-hidden="true">#</a> Tools</h2>`,13),m={href:"https://ffmpeg.guide/",target:"_blank",rel:"noopener noreferrer"};function b(g,v){const s=o("ExternalLinkIcon");return p(),l("div",null,[i,a("p",null,[a("a",d,[e("x265"),n(s)]),e(" is a H.265/HEVC encoder.")]),h,a("p",null,[a("a",m,[e("Build FFmpeg filters"),n(s)]),e(": A simple GUI tool to create complex FFmpeg filter-graphs quickly and correctly")])])}const f=r(c,[["render",b],["__file","FFmpeg.html.vue"]]);export{f as default};
