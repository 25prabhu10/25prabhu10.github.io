import{_ as e,o as t,c as a,e as n}from"./app-jDLxG9B3.js";const r={},i=n(`<h1 id="next-js" tabindex="-1"><a class="header-anchor" href="#next-js" aria-hidden="true">#</a> Next.js</h1><p>Web development framework enabling React-based web applications with server-side rendering and generating static websites</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>npx create-next-app@latest <span class="token parameter variable">--typescript</span>
<span class="token comment"># or</span>
<span class="token function">yarn</span> create next-app
<span class="token comment"># or</span>
<span class="token function">pnpm</span> create next-app
</code></pre></div><p>3 types of rendering methods are available:</p><ul><li><a href="#server-side-rendering-ssr">Server-Side Rendering</a></li><li>Static Site Generation</li><li>Client-Side Rendering</li></ul><p>Server-Side Rendering and Static Site Generation are also referred to as <strong>Pre-Rendering</strong> because the fetching of external data and transformation of React components into HTML happens before the result is sent to the client</p><h2 id="server-side-rendering-ssr" tabindex="-1"><a class="header-anchor" href="#server-side-rendering-ssr" aria-hidden="true">#</a> Server-Side Rendering (SSR)</h2><p>With server-side rendering, the HTML of the page is generated on a server for each request. The generated HTML, JSON data, and JavaScript instructions to make the page interactive are then sent to the client</p><p>On the client, the HTML is used to show a fast non-interactive page, while React uses the JSON data and JavaScript instructions to make components interactive (for example, attaching event handlers to a button). This process is called <strong>hydration</strong></p><p>Use <code>getServerSideProps</code> for SSR in Next.js</p><p>Isomorphic, Universal, or SSR (All the same thing!)</p><ul><li>Isomorphic application, runs both server and client. For each request it runs a virtual app, that generates the static pages</li></ul><p>Advantages:</p><ul><li>Improved SEO</li><li>Faster load times</li></ul><h2 id="static-site-generation-ssg" tabindex="-1"><a class="header-anchor" href="#static-site-generation-ssg" aria-hidden="true">#</a> Static Site Generation (SSG)</h2><p>With Static Site Generation, the HTML is generated on the server, but unlike server-side rendering, there is no server at runtime. Instead, content is generated once, at build time, when the application is deployed, and the HTML is stored in a CDN and re-used for each request</p><p>To statically generate pages use <code>getStaticProps</code></p><h2 id="client-side-rendering" tabindex="-1"><a class="header-anchor" href="#client-side-rendering" aria-hidden="true">#</a> Client-Side Rendering</h2><p>In a standard React application, the browser receives an empty HTML shell from the server along with the JavaScript instructions to construct the UI. This is called client-side rendering because the initial rendering work happens on the user&#39;s device</p><h2 id="incremental-static-regeneration-isr" tabindex="-1"><a class="header-anchor" href="#incremental-static-regeneration-isr" aria-hidden="true">#</a> Incremental Static Regeneration (ISR)</h2><p>You can use <strong>Incremental Static Regeneration</strong> to create or update static pages after you’ve built your site. This means you do not have to rebuild your entire site if your data changes</p>`,21),s=[i];function o(c,d){return t(),a("div",null,s)}const l=e(r,[["render",o],["__file","Next.js.html.vue"]]);export{l as default};
